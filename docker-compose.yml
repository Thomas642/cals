services:
  db:
    image: postgis/postgis:16-3.4
    container_name: ft_db
    restart: unless-stopped
    environment:
      POSTGRES_DB:       familytracker
      POSTGRES_USER:     familytracker
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U familytracker"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build: ./backend
    container_name: ft_api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV:        production
      PORT:            3000
      DB_HOST:         db
      DB_PORT:         "5432"
      DB_USER:         familytracker
      DB_PASSWORD:     ${DB_PASSWORD}
      DB_NAME:         familytracker
      JWT_SECRET:      ${JWT_SECRET}
      JWT_EXPIRES_IN:  30d
      VAPID_PUBLIC_KEY:  ${VAPID_PUBLIC_KEY}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY}
      VAPID_EMAIL:       ${VAPID_EMAIL}
      FRONTEND_URL:    ${FRONTEND_URL}
      UPLOAD_DIR:      /app/uploads
    volumes:
      - uploads:/app/uploads
    expose:
      - "3000"
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', r => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 5s
      retries: 3

  nginx:
    image: nginx:1.27-alpine
    container_name: ft_nginx
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/familytracker.conf:/etc/nginx/conf.d/default.conf:ro
      - ./frontend:/usr/share/nginx/html:ro
      - uploads:/var/uploads:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://localhost/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  db_data:
  uploads:
