<?xml version="1.0" encoding="utf-8"?>
<!--
  Permissions to add INSIDE <manifest> in android/app/src/main/AndroidManifest.xml
  The patch-android.bat script adds these automatically after `cap add android`.
-->
    <!-- Location (foreground) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Location (background — required for GPS when screen is off) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service (runs the GPS background worker) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Keep CPU/network alive for the foreground service -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
